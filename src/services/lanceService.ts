import { Lance } from '../models/Lance';

export const lanceService = {
  criarLance: async (novoLance: Lance) => {
    // Implemente a lógica para criar um lance no banco de dados
    // Use o Prisma para interagir com o banco de dados
    
  },
};